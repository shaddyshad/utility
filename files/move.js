const fs = require('fs');
const path = require('path');

/*
* @param src {List} - a list of files to move
* @param dest {String} - the directory to move the files
 */
function move_to_folder(src, dest){
    const destDir = path.resolve(dest);
    src.forEach(function (file) {

    })

}